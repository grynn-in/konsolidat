# Scoped consolidation (dbt) — PRD backlog (ralph loop)

Follow-ups from merged PRs #112/#115 (all non-blocking). Context + edit/test/commit workflow + STATE SAFETY: `DBT-HANDOFF.md`. Branch `feat/scoped-consolidation`. One PRD per agent; TDD via dbt tests; each reviewed ≤2× then fixed. Order matters: A1 → A2 → C1 → C2.

## A — finish scoped consolidation
- [x] **A1 — #119: scope/period coverage for cash-flow + YTD.** `gold_cash_flow_indirect.sql` and `gold_ytd_trial_balance.sql` `ref('gold_trial_balance')` directly, so a scoped close (`entity_scope`/`fiscal_year`/`fiscal_period` vars) narrows the consolidated TB but NOT these two → inconsistent scoped output. Apply the same `period_filter`/`scope_filter` predicates to both models (or re-source them through the already-filtered chokepoint) so a scoped run narrows them consistently. Add validation/guard if a scope code resolves to zero entities (don't silently emit empty). **TDD:** a singular test asserting that, under a scope var, cash-flow/YTD rows are confined to the scoped entities (and that a no-var build is unchanged). Restore full counts after.
- [x] **A2 — #116: incremental-by-period materialization.** Scoped closes currently OVERWRITE the full consolidated tables. Make the chokepoint (`gold_consolidated_trial_balance` and the affected downstream `gold_fully_consolidated_tb`/cash-flow/YTD) `materialized='incremental'` with `incremental_strategy='delete+insert'` keyed on the close slice (`fiscal_year`[, `fiscal_period`], and entity scope) so a scoped close updates ONLY its slice and leaves other slices intact; full `--full-refresh` (or no-var) rebuilds everything. **TDD:** build full → run a scoped incremental for one (year, scope) → assert that slice updated AND other slices' rows untouched (not zeroed). This is the larger PRD — keep the no-var/full path byte-for-byte equivalent. Restore full state after.
- [x] **A3 — reconcile A1 confinement test with A2 incremental semantics.** A2 made cash-flow/YTD `incremental` (delete+insert by slice), so a scoped close now PRESERVES out-of-scope slices by design. A1's `tests/assert_scoped_cash_flow_ytd_confined.sql` checks the WHOLE persisted table for out-of-scope `data_area_id`, so under a scoped run it now false-fails (122 retained rows) — whole-table confinement is no longer the invariant. Fix the test to assert the REAL invariant: the **freshly-closed slice** contains only in-scope entities — i.e. apply `period_filter` to the offenders queries so confinement is checked only for the closed `(fiscal_year[, fiscal_period])` rows (the slice the close actually rewrites), not every historical slice. Keep it opt-in (no-var → 0 rows). **TDD:** RED = current test false-fails under a scoped incremental run (e.g. `entity_scope=DEMF fiscal_year=2024` after a full build); GREEN after the fix; the A2 slice-preservation test (`assert_incremental_slice_preserved`) still passes; no-var build unchanged. Restore full state after.

## C — hygiene
- [x] **C1 — #118: GL sign refactor cleanup.** After #112, silver derives debit/credit from the SIGN of `accounting_currency_amount`; `is_credit` no longer drives the split. Retire the now-dead `is_credit` (drop the column or stop selecting it), fix the stale comment(s) that still describe the old is_credit logic, and escalate the GL balance singular test (warn → error, or tighten) so an unbalanced GL fails the build. **TDD:** the balance test errors on a deliberately-unbalanced fixture row and passes on real data; assert is_credit is gone from the model output.
- [x] **C2 — #120: equity-FX cleanup.** 12 acquisition-date `exchange_rates` rows have no consumer (equity translation reads `historical_equity_rates`; `rate_lookup` doesn't read them) — drop/quarantine them or document why retained. Add the missing AMG IAS-21 coverage noted in #120. **TDD:** assert no model references the dropped rows and the AMG entities have equity-translation coverage. Restore full state after.

## Done
- **C2 — #120** (commit 4d0cb91): equity-FX cleanup. Same pattern as A1/C1 — the data work was ALREADY merged upstream into origin/main via #104/#121 (AMG `historical_equity_rates` block AMHQ/AMUS/AMDE×3010/3100 keyed `AMG`; Contoso block re-keyed `GROUP_EMEA`→`GROUP_CORP` per the #104 review; 12 dead acquisition-date `exchange_rates` rows dropped from the demo generator). The genuinely-missing piece — the TDD gate — was added as one singular test `tests/assert_equity_rate_coverage.sql`: a source/seed contract that returns an offender per (a) `consolidation_groups` seed subsidiary with NO `historical_equity_rates` coverage (`missing_equity_rate_coverage` — the #120 "AMG has equity coverage" check + catches the #104 GROUP_EMEA-dead-key bug) and (b) a `historical_equity_rates` (group,entity) matching no seed entity (`orphan_equity_rate` — the #120(b) no-dead-rows guard). No model SQL touched ⇒ full builds byte-for-byte. **Live data is REAL D365** (gold = GROUP_CORP × DEMF/JPMF/USMF, no AMG, no 3010/3100 equity accounts), and the live `epm_staging.historical_equity_rates` source was STALE (8 rows: DEMF/GBMF mis-keyed GROUP_EMEA + JPMF/USMF GROUP_CORP, AMG absent). RED FAIL 7 (5 missing AMHQ/AMUS/AMDE/DEMF/GBMF + 2 orphan GROUP_EMEA/DEMF,GBMF). Implement = surgically correct the live source to origin/main's 14-row state (`ALTER…DELETE WHERE consolidation_group='GROUP_EMEA'` drops the 4 dead rows; kept the already-correct GROUP_CORP/USMF+JPMF; INSERT the 10 missing GROUP_CORP DEMF/GBMF + 6 AMG). GREEN PASS=0. The equity join is INERT on real-D365 gold (no 3010/3100 → 0 rows carry `historical_equity_rate`), so a no-var `--full-refresh` of `gold_consolidated_trial_balance+` left every count at baseline **13483/13954/5334/31102** (state restored). NOTE: the 12 dropped `exchange_rates` rows are a demo-generator change with NO live manifestation (live `epm_raw.exchange_rates` is real D365, PascalCase, 19026 rows) — the `orphan_equity_rate` clause is the live-testable proxy for "no dead/dropped rate rows".
- **C1 — #118** (commit 8c16d35): the model-side cleanup was ALREADY merged upstream into origin/main (is_credit retired across stg_d365/canonical/erpnext/bronze/silver + dimension_helpers doc comment; GL balance test `assert_silver_gl_debit_credit_balance` escalated to `severity='error'`). Same pattern as A1: the LIVE open_epm dbt_project tree was STALE (still carried is_credit + warn) and got those 8 files synced back in from origin/main. The genuinely-missing piece — the TDD gate — was added as a new singular test `tests/assert_is_credit_retired.sql` asserting is_credit is gone from the model output (checks `system.columns` for the relation behind `ref('stg_gl_entries')` and `ref('bronze_general_journal_account_entries')`). RED FAIL 2 against the stale built tables (is_credit on the canonical view + bronze table) → GREEN PASS after a `--full-refresh` of the GL chain dropped the column. Balance test (now error) PASS on 455,812 live rows (0 imbalance). No data change: silver=455812 + gold 13483/13954/5334/31102 unchanged (is_credit was carried-but-unused ⇒ removal is byte-equivalent). State restored to baseline.
- **A3 — reconcile** (commit 9ad34db): A2 made cash-flow/YTD `incremental` (delete+insert by slice), so a scoped close now PRESERVES out-of-scope sibling slices by design — A1's `assert_scoped_cash_flow_ytd_confined` scanned the WHOLE persisted table and false-failed (FAIL 122 after a DEMF/2024 scoped close). Empirically proved period-bounding the persisted scan does NOT reach 0 either: 30 sibling entities share fiscal_year 2024 (cf=937 / ytd=5256 offenders). Entity presence in the persisted table cannot tell a freshly-closed row from a preserved sibling. Rewrote the test to assert the REAL invariant on the FRESHLY-WRITTEN slice = each model's SOURCE projection (`gold_trial_balance` through the SAME `period_filter`+`scope_filter` it applies — cash-flow single-period, YTD `include_period=false`); `scoped_entities` resolved independently of the `scope_filter` macro so a macro over-selection regression still surfaces. Opt-in unchanged (no var ⇒ 0 rows). RED (old test) FAIL 122 → GREEN (new test) PASS for DEMF / DEMF+2024 / DEMF+2024+P6 / GROUP_CORP / GROUP_EMEA / no-var; A2 `assert_incremental_slice_preserved` still PASS. Live gold restored to baseline 13483/13954/5334/31102.
- **A2 — #116** (commit e59f83f): made the 4 consolidation models (`gold_consolidated_trial_balance`, `gold_fully_consolidated_tb`, `gold_cash_flow_indirect`, `gold_ytd_trial_balance`) `materialized='incremental'` + `incremental_strategy='delete+insert'`, keyed on the close slice — `(consolidation_group, data_area_id, fiscal_year, fiscal_period)` for the two TBs, `(data_area_id, fiscal_year, fiscal_period)` for cash-flow/YTD. A scoped close now delete+inserts only its in-scope keys; other slices survive. No-var/full-refresh touches every key ⇒ byte-for-byte equal to the old `table`. TDD gate `tests/assert_incremental_slice_preserved.sql` (opt-in var `assert_preserved_entity`, distinct from `entity_scope`): RED on old table mat (scoped DEMF/2024 close wiped USMF → FAIL 4) → GREEN after incremental (USMF preserved). Slice-updated proof: deleted the DEMF/2024 cash-flow slice, scoped re-run rebuilt it to 107 while USMF stayed 948. State restored to baseline 13483/13954/5334/31102. **Follow-up (file as new issue): A2's preservation contract conflicts with A1's whole-table confinement test (`assert_scoped_cash_flow_ytd_confined`) when run WITH `entity_scope` — incremental keeps other entities, so that A1 guard then FAILs (122 rows). No-var path unaffected; only a scoped full-suite `dbt build` trips it. A1 confinement must be reconciled to check the freshly-written slice, not the persisted table.**
- **A1 — #119** (commit 7ea7bb3): model filters were already merged via #121 (cash-flow `period_filter()`/`scope_filter()`; YTD `period_filter(include_period=false)`/`scope_filter()`). Added the two missing TDD gates: `tests/assert_scoped_cash_flow_ytd_confined.sql` (RED→GREEN: under `entity_scope`, both bypass models confined to the scope) and `tests/assert_scope_resolves_to_entities.sql` (zero-entity scope fails the build, not silent-empty). Both opt-in (no rows with no var). Live counts restored: cash_flow=5334, ytd=31102.
